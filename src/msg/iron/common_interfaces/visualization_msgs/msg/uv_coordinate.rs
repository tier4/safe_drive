// This file was automatically generated by ros2msg_to_rs (https://github.com/tier4/ros2msg_to_rs).
use super::*;
use super::super::super::*;
use crate::msg::*;
use crate::rcl;

extern "C" {
    fn visualization_msgs__msg__UVCoordinate__init(msg: *mut UVCoordinate) -> bool;
    fn visualization_msgs__msg__UVCoordinate__fini(msg: *mut UVCoordinate);
    fn visualization_msgs__msg__UVCoordinate__are_equal(lhs: *const UVCoordinate, rhs: *const UVCoordinate) -> bool;
    fn visualization_msgs__msg__UVCoordinate__Sequence__init(msg: *mut UVCoordinateSeqRaw, size: usize) -> bool;
    fn visualization_msgs__msg__UVCoordinate__Sequence__fini(msg: *mut UVCoordinateSeqRaw);
    fn visualization_msgs__msg__UVCoordinate__Sequence__are_equal(lhs: *const UVCoordinateSeqRaw, rhs: *const UVCoordinateSeqRaw) -> bool;
    fn rosidl_typesupport_c__get_message_type_support_handle__visualization_msgs__msg__UVCoordinate() -> *const rcl::rosidl_message_type_support_t;
}


#[repr(C)]
#[derive(Debug)]
pub struct UVCoordinate {
    pub u: f32,
    pub v: f32,
}

impl UVCoordinate {
    pub fn new() -> Option<Self> {
        let mut msg: Self = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        if unsafe { visualization_msgs__msg__UVCoordinate__init(&mut msg) } {
            Some(msg)
        } else {
            None
        }
    }
}

impl Drop for UVCoordinate {
    fn drop(&mut self) {
        unsafe { visualization_msgs__msg__UVCoordinate__fini(self) };
    }
}

#[repr(C)]
#[derive(Debug)]
struct UVCoordinateSeqRaw {
    data: *mut UVCoordinate,
    size: usize,
    capacity: usize,
}

/// Sequence of UVCoordinate.
/// `N` is the maximum number of elements.
/// If `N` is `0`, the size is unlimited.
#[repr(C)]
#[derive(Debug)]
pub struct UVCoordinateSeq<const N: usize> {
    data: *mut UVCoordinate,
    size: usize,
    capacity: usize,
}

impl<const N: usize> UVCoordinateSeq<N> {
    /// Create a sequence of.
    /// `N` represents the maximum number of elements.
    /// If `N` is `0`, the sequence is unlimited.
    pub fn new(size: usize) -> Option<Self> {
        if N != 0 && size >= N {
            // the size exceeds in the maximum number
            return None;
        }

        let mut msg: UVCoordinateSeqRaw = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        if unsafe { visualization_msgs__msg__UVCoordinate__Sequence__init(&mut msg, size) } {
            Some(Self {data: msg.data, size: msg.size, capacity: msg.capacity })
        } else {
            None
        }
    }

    pub fn null() -> Self {
        let msg: UVCoordinateSeqRaw = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        Self {data: msg.data, size: msg.size, capacity: msg.capacity }
    }

    pub fn as_slice(&self) -> &[UVCoordinate] {
        if self.data.is_null() {
            &[]
        } else {
            let s = unsafe { std::slice::from_raw_parts(self.data, self.size) };
            s
        }
    }

    pub fn as_slice_mut(&mut self) -> &mut [UVCoordinate] {
        if self.data.is_null() {
            &mut []
        } else {
            let s = unsafe { std::slice::from_raw_parts_mut(self.data, self.size) };
            s
        }
    }

    pub fn iter(&self) -> std::slice::Iter<'_, UVCoordinate> {
        self.as_slice().iter()
    }

    pub fn iter_mut(&mut self) -> std::slice::IterMut<'_, UVCoordinate> {
        self.as_slice_mut().iter_mut()
    }

    pub fn len(&self) -> usize {
        self.as_slice().len()
    }

    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }
}

impl<const N: usize> Drop for UVCoordinateSeq<N> {
    fn drop(&mut self) {
        let mut msg = UVCoordinateSeqRaw{data: self.data, size: self.size, capacity: self.capacity};
        unsafe { visualization_msgs__msg__UVCoordinate__Sequence__fini(&mut msg) };
    }
}

unsafe impl<const N: usize> Send for UVCoordinateSeq<N> {}
unsafe impl<const N: usize> Sync for UVCoordinateSeq<N> {}


impl TypeSupport for UVCoordinate {
    fn type_support() -> *const rcl::rosidl_message_type_support_t {
        unsafe {
            rosidl_typesupport_c__get_message_type_support_handle__visualization_msgs__msg__UVCoordinate()
        }
    }
}

impl PartialEq for UVCoordinate {
    fn eq(&self, other: &Self) -> bool {
        unsafe {
            visualization_msgs__msg__UVCoordinate__are_equal(self, other)
        }
    }
}

impl<const N: usize> PartialEq for UVCoordinateSeq<N> {
    fn eq(&self, other: &Self) -> bool {
        unsafe {
            let msg1 = UVCoordinateSeqRaw{data: self.data, size: self.size, capacity: self.capacity};
            let msg2 = UVCoordinateSeqRaw{data: other.data, size: other.size, capacity: other.capacity};
            visualization_msgs__msg__UVCoordinate__Sequence__are_equal(&msg1, &msg2)
        }
    }
}

