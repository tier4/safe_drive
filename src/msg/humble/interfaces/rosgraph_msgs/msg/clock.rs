// This file was automatically generated by ros2msg_to_rs (https://github.com/tier4/ros2msg_to_rs).
use super::*;
use super::super::super::*;
use crate::msg::*;
use crate::rcl;

extern "C" {
    fn rosgraph_msgs__msg__Clock__init(msg: *mut Clock) -> bool;
    fn rosgraph_msgs__msg__Clock__fini(msg: *mut Clock);
    fn rosgraph_msgs__msg__Clock__are_equal(lhs: *const Clock, rhs: *const Clock) -> bool;
    fn rosgraph_msgs__msg__Clock__Sequence__init(msg: *mut ClockSeqRaw, size: usize) -> bool;
    fn rosgraph_msgs__msg__Clock__Sequence__fini(msg: *mut ClockSeqRaw);
    fn rosgraph_msgs__msg__Clock__Sequence__are_equal(lhs: *const ClockSeqRaw, rhs: *const ClockSeqRaw) -> bool;
    fn rosidl_typesupport_c__get_message_type_support_handle__rosgraph_msgs__msg__Clock() -> *const rcl::rosidl_message_type_support_t;
}


#[repr(C)]
#[derive(Debug)]
pub struct Clock {
    pub clock: builtin_interfaces::UnsafeTime,
}

impl Clock {
    pub fn new() -> Option<Self> {
        let mut msg: Self = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        if unsafe { rosgraph_msgs__msg__Clock__init(&mut msg) } {
            Some(msg)
        } else {
            None
        }
    }
}

impl Drop for Clock {
    fn drop(&mut self) {
        unsafe { rosgraph_msgs__msg__Clock__fini(self) };
    }
}

#[repr(C)]
#[derive(Debug)]
struct ClockSeqRaw {
    data: *mut Clock,
    size: usize,
    capacity: usize,
}

/// Sequence of Clock.
/// `N` is the maximum number of elements.
/// If `N` is `0`, the size is unlimited.
#[repr(C)]
#[derive(Debug)]
pub struct ClockSeq<const N: usize> {
    data: *mut Clock,
    size: usize,
    capacity: usize,
}

impl<const N: usize> ClockSeq<N> {
    /// Create a sequence of.
    /// `N` represents the maximum number of elements.
    /// If `N` is `0`, the sequence is unlimited.
    pub fn new(size: usize) -> Option<Self> {
        if N != 0 && size >= N {
            // the size exceeds in the maximum number
            return None;
        }

        let mut msg: ClockSeqRaw = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        if unsafe { rosgraph_msgs__msg__Clock__Sequence__init(&mut msg, size) } {
            Some(Self {data: msg.data, size: msg.size, capacity: msg.capacity })
        } else {
            None
        }
    }

    pub fn null() -> Self {
        let msg: ClockSeqRaw = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        Self {data: msg.data, size: msg.size, capacity: msg.capacity }
    }

    pub fn as_slice(&self) -> &[Clock] {
        if self.data.is_null() {
            &[]
        } else {
            let s = unsafe { std::slice::from_raw_parts(self.data, self.size) };
            s
        }
    }

    pub fn as_slice_mut(&mut self) -> &mut [Clock] {
        if self.data.is_null() {
            &mut []
        } else {
            let s = unsafe { std::slice::from_raw_parts_mut(self.data, self.size) };
            s
        }
    }

    pub fn iter(&self) -> std::slice::Iter<'_, Clock> {
        self.as_slice().iter()
    }

    pub fn iter_mut(&mut self) -> std::slice::IterMut<'_, Clock> {
        self.as_slice_mut().iter_mut()
    }

    pub fn len(&self) -> usize {
        self.as_slice().len()
    }

    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }
}

impl<const N: usize> Drop for ClockSeq<N> {
    fn drop(&mut self) {
        let mut msg = ClockSeqRaw{data: self.data, size: self.size, capacity: self.capacity};
        unsafe { rosgraph_msgs__msg__Clock__Sequence__fini(&mut msg) };
    }
}

unsafe impl<const N: usize> Send for ClockSeq<N> {}
unsafe impl<const N: usize> Sync for ClockSeq<N> {}


impl TypeSupport for Clock {
    fn type_support() -> *const rcl::rosidl_message_type_support_t {
        unsafe {
            rosidl_typesupport_c__get_message_type_support_handle__rosgraph_msgs__msg__Clock()
        }
    }
}

impl PartialEq for Clock {
    fn eq(&self, other: &Self) -> bool {
        unsafe {
            rosgraph_msgs__msg__Clock__are_equal(self, other)
        }
    }
}

impl<const N: usize> PartialEq for ClockSeq<N> {
    fn eq(&self, other: &Self) -> bool {
        unsafe {
            let msg1 = ClockSeqRaw{data: self.data, size: self.size, capacity: self.capacity};
            let msg2 = ClockSeqRaw{data: other.data, size: other.size, capacity: other.capacity};
            rosgraph_msgs__msg__Clock__Sequence__are_equal(&msg1, &msg2)
        }
    }
}

