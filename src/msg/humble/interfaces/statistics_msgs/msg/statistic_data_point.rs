// This file was automatically generated by ros2msg_to_rs (https://github.com/tier4/ros2msg_to_rs).
use super::*;
use super::super::super::*;
use crate::msg::*;
use crate::rcl;

extern "C" {
    fn statistics_msgs__msg__StatisticDataPoint__init(msg: *mut StatisticDataPoint) -> bool;
    fn statistics_msgs__msg__StatisticDataPoint__fini(msg: *mut StatisticDataPoint);
    fn statistics_msgs__msg__StatisticDataPoint__are_equal(lhs: *const StatisticDataPoint, rhs: *const StatisticDataPoint) -> bool;
    fn statistics_msgs__msg__StatisticDataPoint__Sequence__init(msg: *mut StatisticDataPointSeqRaw, size: usize) -> bool;
    fn statistics_msgs__msg__StatisticDataPoint__Sequence__fini(msg: *mut StatisticDataPointSeqRaw);
    fn statistics_msgs__msg__StatisticDataPoint__Sequence__are_equal(lhs: *const StatisticDataPointSeqRaw, rhs: *const StatisticDataPointSeqRaw) -> bool;
    fn rosidl_typesupport_c__get_message_type_support_handle__statistics_msgs__msg__StatisticDataPoint() -> *const rcl::rosidl_message_type_support_t;
}


#[repr(C)]
#[derive(Debug)]
pub struct StatisticDataPoint {
    pub data_type: u8,
    pub data: f64,
}

impl StatisticDataPoint {
    pub fn new() -> Option<Self> {
        let mut msg: Self = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        if unsafe { statistics_msgs__msg__StatisticDataPoint__init(&mut msg) } {
            Some(msg)
        } else {
            None
        }
    }
}

impl Drop for StatisticDataPoint {
    fn drop(&mut self) {
        unsafe { statistics_msgs__msg__StatisticDataPoint__fini(self) };
    }
}

#[repr(C)]
#[derive(Debug)]
struct StatisticDataPointSeqRaw {
    data: *mut StatisticDataPoint,
    size: usize,
    capacity: usize,
}

/// Sequence of StatisticDataPoint.
/// `N` is the maximum number of elements.
/// If `N` is `0`, the size is unlimited.
#[repr(C)]
#[derive(Debug)]
pub struct StatisticDataPointSeq<const N: usize> {
    data: *mut StatisticDataPoint,
    size: usize,
    capacity: usize,
}

impl<const N: usize> StatisticDataPointSeq<N> {
    /// Create a sequence of.
    /// `N` represents the maximum number of elements.
    /// If `N` is `0`, the sequence is unlimited.
    pub fn new(size: usize) -> Option<Self> {
        if N != 0 && size >= N {
            // the size exceeds in the maximum number
            return None;
        }

        let mut msg: StatisticDataPointSeqRaw = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        if unsafe { statistics_msgs__msg__StatisticDataPoint__Sequence__init(&mut msg, size) } {
            Some(Self {data: msg.data, size: msg.size, capacity: msg.capacity })
        } else {
            None
        }
    }

    pub fn null() -> Self {
        let msg: StatisticDataPointSeqRaw = unsafe { std::mem::MaybeUninit::zeroed().assume_init() };
        Self {data: msg.data, size: msg.size, capacity: msg.capacity }
    }

    pub fn as_slice(&self) -> &[StatisticDataPoint] {
        if self.data.is_null() {
            &[]
        } else {
            let s = unsafe { std::slice::from_raw_parts(self.data, self.size) };
            s
        }
    }

    pub fn as_slice_mut(&mut self) -> &mut [StatisticDataPoint] {
        if self.data.is_null() {
            &mut []
        } else {
            let s = unsafe { std::slice::from_raw_parts_mut(self.data, self.size) };
            s
        }
    }

    pub fn iter(&self) -> std::slice::Iter<'_, StatisticDataPoint> {
        self.as_slice().iter()
    }

    pub fn iter_mut(&mut self) -> std::slice::IterMut<'_, StatisticDataPoint> {
        self.as_slice_mut().iter_mut()
    }

    pub fn len(&self) -> usize {
        self.as_slice().len()
    }

    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }
}

impl<const N: usize> Drop for StatisticDataPointSeq<N> {
    fn drop(&mut self) {
        let mut msg = StatisticDataPointSeqRaw{data: self.data, size: self.size, capacity: self.capacity};
        unsafe { statistics_msgs__msg__StatisticDataPoint__Sequence__fini(&mut msg) };
    }
}

unsafe impl<const N: usize> Send for StatisticDataPointSeq<N> {}
unsafe impl<const N: usize> Sync for StatisticDataPointSeq<N> {}


impl TypeSupport for StatisticDataPoint {
    fn type_support() -> *const rcl::rosidl_message_type_support_t {
        unsafe {
            rosidl_typesupport_c__get_message_type_support_handle__statistics_msgs__msg__StatisticDataPoint()
        }
    }
}

impl PartialEq for StatisticDataPoint {
    fn eq(&self, other: &Self) -> bool {
        unsafe {
            statistics_msgs__msg__StatisticDataPoint__are_equal(self, other)
        }
    }
}

impl<const N: usize> PartialEq for StatisticDataPointSeq<N> {
    fn eq(&self, other: &Self) -> bool {
        unsafe {
            let msg1 = StatisticDataPointSeqRaw{data: self.data, size: self.size, capacity: self.capacity};
            let msg2 = StatisticDataPointSeqRaw{data: other.data, size: other.size, capacity: other.capacity};
            statistics_msgs__msg__StatisticDataPoint__Sequence__are_equal(&msg1, &msg2)
        }
    }
}

